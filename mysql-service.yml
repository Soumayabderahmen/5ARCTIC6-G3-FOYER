piVersion: v1
kind: Service
metadata:
  name: mysql-devops-db  # Nom modifié en minuscules avec des tirets
spec:
  ports:
    - port: 3306
  selector:
    app: mysql-devops-db  # Nom modifié pour correspondre à celui du déploiement